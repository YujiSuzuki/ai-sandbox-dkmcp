# Copy this file to .env and fill in the values

PORT=8080
NODE_ENV=development

# Path to secret files (inside container)
JWT_SECRET_PATH=/app/secrets/jwt-secret.key
ENCRYPTION_KEY_PATH=/app/secrets/encryption.key

# Database (for demo, we use in-memory)
# DATABASE_URL=postgresql://user:password@db/securenote
